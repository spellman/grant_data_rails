<%= provide :title, "Edit record" %>

<%= render "shared/error_messages", resource: @record %>
<%= form_for @record do |f| %>
  <%= f.label "name", "Name" %>
  <%= f.text_field "name" %>
  <%= f.submit "save" %>
<% end %>
