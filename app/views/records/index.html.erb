<%= will_paginate %>
  <table class="records">
    <colgroup>
      <col class="records_column_1">
      <col class="records_column_2">
    </colgroup>
    <tr>
      <th>Name</th>
    </tr>
    <% @records.each do |record| %>
      <tr>
        <td><%= record.name %></td>
      </tr>
    <% end %>
  </table>
<%= will_paginate %>
