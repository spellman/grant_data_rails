<%= will_paginate %>
  <ul class="users">
    <% @users.each do |user| %>
      <li>
        <%= user.email %>
        <%= link_to "delete", user, method: :delete, data: { confirm: "Delete for realsies?" } %>
      </li>
    <% end %>
  </ul>
<%= will_paginate %>

<%= link_to "Create new user", new_user_path %>
