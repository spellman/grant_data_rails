<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= user.email %>
      <%= link_to "delete", user, method: :delete, data: { confirm: "For realsies?" } %>
    </li>
  <% end %>
</ul>

<%= link_to "Create new user", new_user_path %>
