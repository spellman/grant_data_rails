<% provide(:title, "Edit user") %>

<% provide(:full_width_top) do %>
  <div class="container">
    <div class="page-header">
      <h1>Edit <%= @user.email %></h1>
    </div>
  </div>
<% end %>

<% provide(:resource_form) do %>
  <%= render(partial: "users/user_edit",
             locals: {cancel_edit_path: cancel_edit_path,
                      cancel_link_remote: cancel_link_remote}) %>
<% end %>

<%= render(template: "layouts/form_and_display_page") %>
